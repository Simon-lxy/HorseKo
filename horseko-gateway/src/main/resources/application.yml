server:
  port: 5000
spring:
  cloud:
    gateway:
      routes:
        - id: oss
          uri: lb://horseko-oss
          predicates:
            - Path=/oss/**
          filters:
            - StripPrefix=1
